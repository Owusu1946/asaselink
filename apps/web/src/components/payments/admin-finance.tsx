"use client";

import { useState, useTransition } from "react";
import { Button } from "@asaselink/ui/components/button";
import { client } from "@/utils/orpc";
import { notify } from "@/utils/notify";

type Queue = { payments: Record<string, unknown>[]; payouts: Record<string, unknown>[] };

export function AdminFinance({ initialQueue }: { initialQueue: Queue }) {
  const [queue, setQueue] = useState(initialQueue);
  const [reason, setReason] = useState("Mock transaction verified by operations");
  const [pending, startTransition] = useTransition();
  const refresh = async () => setQueue(await client.payments.adminQueue() as Queue);
  const reviewPayment = (paymentReference: string, decision: "APPROVE" | "REJECT") => startTransition(async () => { try { await client.payments.reviewPayment({ paymentReference, decision, reason }); await refresh(); notify.success(decision === "APPROVE" ? "Purchase confirmed" : "Payment rejected"); } catch (error) { notify.apiError(error, "Payment review failed"); } });
  const reviewPayout = (payoutReference: string, decision: "MARK_PAID" | "REJECT") => startTransition(async () => { try { await client.payments.reviewPayout({ payoutReference, decision, reason }); await refresh(); notify.success(decision === "MARK_PAID" ? "Payout marked paid" : "Payout rejected"); } catch (error) { notify.apiError(error, "Payout review failed"); } });
  return <><div><h1 className="text-2xl font-bold tracking-tight sm:text-3xl">Payments & payouts</h1><p className="mt-1 text-sm text-muted-foreground">Verify mock collections before plots are sold and seller balances are released.</p></div><label className="block max-w-xl text-xs font-semibold">Required audit note<input value={reason} onChange={(event) => setReason(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-border bg-background px-3 text-sm font-normal" /></label><section><h2 className="mb-3 text-lg font-semibold">Payment verification</h2><div className="grid gap-3">{queue.payments.map((payment) => <article key={String(payment.reference)} className="rounded-2xl border border-border bg-card p-5"><div className="grid gap-4 sm:grid-cols-[1fr_auto]"><div><p className="font-semibold">{String(payment.estateName)} · {String(payment.plotNumber)}</p><p className="mt-1 text-xs text-muted-foreground">{String(payment.buyerEmail)} · {String(payment.method).replaceAll("_", " ")}</p><p className="mt-2 font-mono text-xs">{String(payment.reference)}</p>{payment.bankTransferReference ? <p className="mt-1 text-xs">Transfer: {String(payment.bankTransferReference)}</p> : null}</div><div className="sm:text-right"><p className="text-lg font-bold">GHS {Number(payment.amount).toLocaleString()}</p><p className="text-xs text-muted-foreground">{String(payment.status).replaceAll("_", " ")}</p>{payment.status === "PENDING_CONFIRMATION" ? <div className="mt-3 flex gap-2"><Button size="sm" disabled={pending || reason.length < 5} onClick={() => reviewPayment(String(payment.reference), "APPROVE")}>Verify & sell</Button><Button size="sm" variant="outline" disabled={pending || reason.length < 5} onClick={() => reviewPayment(String(payment.reference), "REJECT")}>Reject</Button></div> : null}</div></div></article>)}</div></section><section><h2 className="mb-3 text-lg font-semibold">Payout requests</h2><div className="grid gap-3">{queue.payouts.length ? queue.payouts.map((payout) => <article key={String(payout.reference)} className="rounded-2xl border border-border bg-card p-5"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="font-semibold">{String(payout.companyName)}</p><p className="mt-1 font-mono text-xs text-muted-foreground">{String(payout.reference)}</p></div><div><p className="font-bold">GHS {Number(payout.amount).toLocaleString()}</p><p className="text-xs text-muted-foreground">{String(payout.status)}</p></div>{payout.status === "REQUESTED" ? <div className="flex gap-2"><Button size="sm" disabled={pending || reason.length < 5} onClick={() => reviewPayout(String(payout.reference), "MARK_PAID")}>Mark paid</Button><Button size="sm" variant="outline" disabled={pending || reason.length < 5} onClick={() => reviewPayout(String(payout.reference), "REJECT")}>Reject</Button></div> : null}</div></article>) : <div className="rounded-2xl border border-dashed border-border p-8 text-center text-sm text-muted-foreground">No payout requests.</div>}</div></section></>;
}
